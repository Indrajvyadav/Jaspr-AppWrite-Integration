import 'package:jaspr/jaspr.dart';

class Theme {
  static const EdgeInsets eZero = EdgeInsets.zero;

  static const Unit zero = Unit.zero;
  static const Unit auto = Unit.auto;
  static const Color trans = Colors.transparent;
  static const Color white = Colors.white;
}
